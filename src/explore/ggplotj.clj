(ns explore.ggplotj
  (:refer-clojure :exclude [+]))

;; PROTOCOLS AND STRUCTS!
;;
;; as long as we don't need to dispatch on two arguments---then we need multimethods / a "defgeneric"

(defn + [])
(defn ggplot [])
(defn aes [])
(defn geom-point [])


(comment
  +

  )
